function add(param1, param2, param3) {
  return param1 + param2 + param3;
  // console.log('post return');
}

const arg1 = 1;
const arg2 = 2;
const arg3 = 3;

const addResponse = add(arg1, arg2, arg3);
console.log(addResponse); // 6

// aplicar la función getIfIsPrime en el ejercicio de los números primos
// esta función recibirá un número como param y devolverá un booleano
